FROM node:20-alpine

WORKDIR /app

RUN apk add --no-cache ca-certificates openssl && update-ca-certificates

COPY package*.json ./
RUN npm ci

EXPOSE 3000

CMD ["sh", "-c", "export WP_CACHE_BUILD_KEY=\"${WP_CACHE_BUILD_KEY:-$(date +%s)}\"; npx next dev --hostname 0.0.0.0"]
